flag 1: SELECT id, username FROM users WHERE id=1 UNION SELECT id, password FROM users LIMIT 1;
To sanitize:
$stmt = $pdo->prepare("SELECT id, username FROM users WHERE id = ?");
$stmt->execute([$user_input]);

